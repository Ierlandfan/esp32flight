CONFIG_CANFLIGHT_BOARD_SUNTON_4827S043R=y
# Downscaled font tiers for the 480x272 panel
CONFIG_LV_FONT_MONTSERRAT_8=y
CONFIG_LV_FONT_MONTSERRAT_10=y
# 272 rows: fb size must be an even multiple of the bounce buffer (2*17 divides 272)
CONFIG_EXAMPLE_LCD_RGB_BOUNCE_BUFFER_HEIGHT=17
# Keep internal RAM in large contiguous blocks (task stacks, esp-aes DMA)
CONFIG_SPIRAM_MALLOC_ALWAYSINTERNAL=512
CONFIG_ESP_WIFI_STATIC_RX_BUFFER_NUM=8
